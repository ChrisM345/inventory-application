<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="header">
      <h1><%= title %></h1>
      <% if (locals.search) {%>
      <h2>Search Term: <%= search %></h2>
      <% } %>
      <a href="/trainer">Create a Trainer</a>
      <br />
      <a href="/">Return to Home</a>
    </div>
    <div class="search">
      <form action="/searchName" method="get">
        <label for="pokemonName">Search Pokemon Name: </label>
        <input type="text" name="pokemonName" id="pokemonName" />
        <button type="submit">Search Pokemon Name</button>
      </form>
      <form action="/searchType" method="get">
        <label for="pokemonType">Search Pokemon Type: </label>
        <input type="text" name="pokemonType" id="pokemonType" />
        <button type="submit">Search Pokemon Type</button>
      </form>
    </div>
    <div class="pokemonCardContainer">
      <% data.forEach((pokemon) => { %>
      <div class="pokemonCard">
        <h2><%= pokemon.name %></h2>
        <p>Pokemon ID: <%=(pokemon.id).toString().padStart(3, '0') %></p>
        <form action="pokemon/<%= pokemon.id %>/<%= pokemon.name%>" method="get">
          <input type="submit" value="Details" />
        </form>
        <img className="pokemonImage" src="<%= pokemon.img_url%>" />
        <p>Types: <%= pokemon.types %></p>
      </div>
      <% }) %>
    </div>
  </body>
</html>
